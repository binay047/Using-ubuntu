## 1. Update Google Chrome

Update Google Chrome on Ubuntu using the APT package manager. First, refresh the package lists, then upgrade the installed Google Chrome package to the latest available version.

```bash
sudo apt update
sudo apt upgrade google-chrome-stable
```

These commands update **Google Chrome Stable** to the latest version available from the configured Google Chrome repository.
